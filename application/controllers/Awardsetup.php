<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Awardsetup extends CI_Controller
{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Awardcat_model');
        $this->load->library('form_validation');
        $this->load->library('datatables');
    }

    public function index()
    {
        if (isExistClassMenu() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $this->template->general('awardsetup/award_cat_list');
    }

    public function json()
    {
        header('Content-Type: application/json');
        echo $this->Awardcat_model->json();
    }

    public function read($id)
    {
        if (isAdmin() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $row = $this->Awardcat_model->get_by_id($id);
        if ($row) {
            $data = array(
                'button' => 'Read',
                'action' => 'read',
                'id' => $row->id,
                'award_name' => $row->award_name,
            );
            $this->template->general('awardsetup/award_cat_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('awardsetup'));
        }
    }

    public function create()
    {
        if (isExistClassMenu() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $data = array(
            'button' => 'Create',
            'action' => site_url('awardsetup/create_action'),
            'id' => set_value('id'),
            'award_name' => set_value('award_name'),
        );
        $this->template->general('awardsetup/award_cat_form', $data);
    }

    public function create_action()
    {
        if (isExistClassMenu() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
                'award_name' => $this->input->post('award_name', TRUE),
            );

            $this->Awardcat_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('awardsetup'));
        }
    }

    public function update($id)
    {
        if (isExistClassMenu() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $row = $this->Awardcat_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('awardsetup/update_action'),
                'id' => set_value('id', $row->id),
                'award_name' => set_value('award_name', $row->award_name),
            );
            $this->template->general('awardsetup/award_cat_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('awardsetup'));
        }
    }

    public function update_action()
    {
        if (isExistClassMenu() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('id', TRUE));
        } else {
            $data = array(
                'award_name' => $this->input->post('award_name', TRUE),
            );

            $this->Awardcat_model->update($this->input->post('id', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('awardsetup'));
        }
    }

    public function delete($id)
    {
        if (isExistClassMenu() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $row = $this->Awardcat_model->get_by_id($id);

        if ($row) {
            $this->Awardcat_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('awardsetup'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('awardsetup'));
        }
    }

    public function _rules()
    {
        $this->form_validation->set_rules('award_name', 'award name', 'trim|required');

        $this->form_validation->set_rules('id', 'id', 'trim');
        $this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }
}

/* End of file Awardsetup.php */
/* Location: ./application/controllers/Awardsetup.php */
/* Please DO NOT modify this information : */
/* Generated by Harviacode Codeigniter CRUD Generator 2019-12-02 15:50:59 */
/* http://harviacode.com */