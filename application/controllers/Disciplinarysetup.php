<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Disciplinarysetup extends CI_Controller
{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Disciplecat_model');
        $this->load->library('form_validation');
        $this->load->library('datatables');
    }

    public function index()
    {
        if (isExistClassMenu() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $this->template->general('diciplinarysetup/disciple_cat_list');
    }

    public function json()
    {
        header('Content-Type: application/json');
        echo $this->Disciplecat_model->json();
    }

    public function read($id)
    {
        if (isExistClassMenu() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $row = $this->Disciplecat_model->get_by_id($id);
        if ($row) {
            $data = array(
                'button' => 'Read',
                'action' => 'read',
                'id' => $row->id,
                'disciple_name' => $row->disciple_name,
            );
            $this->template->general('diciplinarysetup/disciple_cat_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('diciplinarysetup'));
        }
    }

    public function create()
    {
        if (isExistClassMenu() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $data = array(
            'button' => 'Create',
            'action' => site_url('diciplinarysetup/create_action'),
            'id' => set_value('id'),
            'disciple_name' => set_value('disciple_name'),
        );
        $this->template->general('diciplinarysetup/disciple_cat_form', $data);
    }

    public function create_action()
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
                'disciple_name' => $this->input->post('disciple_name', TRUE),
            );

            $this->Disciplecat_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('diciplinarysetup'));
        }
    }

    public function update($id)
    {
        if (isExistClassMenu() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $row = $this->Disciplecat_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('diciplinarysetup/update_action'),
                'id' => set_value('id', $row->id),
                'disciple_name' => set_value('disciple_name', $row->disciple_name),
            );
            $this->template->general('diciplinarysetup/disciple_cat_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('diciplinarysetup'));
        }
    }

    public function update_action()
    {
        if (isExistClassMenu() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('id', TRUE));
        } else {
            $data = array(
                'disciple_name' => $this->input->post('disciple_name', TRUE),
            );

            $this->Disciplecat_model->update($this->input->post('id', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('diciplinarysetup'));
        }
    }

    public function delete($id)
    {
        if (isExistClassMenu() == FALSE) {
            redirect(site_url('dashboard'));
        }

        $row = $this->Disciplecat_model->get_by_id($id);

        if ($row) {
            $this->Disciplecat_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('diciplinarysetup'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('diciplinarysetup'));
        }
    }

    public function _rules()
    {
        $this->form_validation->set_rules('disciple_name', 'disciple name', 'trim|required');

        $this->form_validation->set_rules('id', 'id', 'trim');
        $this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }
}

/* End of file Diciplinarysetup.php */
/* Location: ./application/controllers/Diciplinarysetup.php */
/* Please DO NOT modify this information : */
/* Generated by Harviacode Codeigniter CRUD Generator 2019-12-02 15:58:59 */
/* http://harviacode.com */